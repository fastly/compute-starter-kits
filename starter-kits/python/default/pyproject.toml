[project]
name = "fastly-compute-python-app"
version = "0.1.0"
description = "A basic starter kit for Python on Fastly Compute"
requires-python = ">=3.12"
dependencies = [
    "flask>=3.1.3",
    "fastly-compute>=0.1.3",
]

[tool.fastly-compute]
entry = "main"
