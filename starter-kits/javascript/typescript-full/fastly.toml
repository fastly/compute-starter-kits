# This file describes a Fastly Compute package. To learn more visit:
# https://developer.fastly.com/reference/fastly-toml/

authors = ["<oss@fastly.com>"]
description = "A basic starter kit that demonstrates the use of the TypeScript compiler `tsc`."
language = "javascript"
manifest_version = 3
name = "Transpiled TypeScript starter"

[catalog]
slug = "compute-starter-kit-typescript-full"
tags = ["typescript"]

[scripts]
build = "npm run build"
post_init = "npm install"
